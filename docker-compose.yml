services:

  db:
    image: postgres
    restart: always
    environment:
      POSTGRES_PASSWORD: example
    volumes:
      - ./postgres/data:/var/lib/postgresql/data
    ports:
      - 5432:5432


